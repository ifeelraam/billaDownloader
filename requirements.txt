# Telegram Bot library (latest version)
python-telegram-bot==20.0

# Instagram downloader
instaloader==4.7.6  # Ensure this version is compatible

# Spotify downloader (Spotify API client)
spotipy==2.22.1    # Stable version of the Spotify API client

# For downloading YouTube videos (pytube as a fallback)
pytube==12.1.0  # Stable version of pytube

# Facebook SDK for Facebook API interaction
facebook-sdk==3.1.0

# BeautifulSoup for scraping Gaana, JioSaavn, or other unsupported APIs
beautifulsoup4==4.12.0

# For managing environment variables (.env files)
python-dotenv==0.21.0

# Requests library (for HTTP requests)
requests==2.28.2