# Telegram bot API
python-telegram-bot==20.0

# HTTP requests library
requests==2.31.0

# yt-dlp (for YouTube, Facebook, Instagram, etc. video downloads)
yt-dlp==2023.12.30

# PyTube (for YouTube video downloads)
pytube==12.1.0

# Spotify API library
spotipy==2.18.0

# Instagram downloader library
instaloader==4.14.1

# Facebook scraper library
facebook-scraper==0.2.59

# BeautifulSoup for scraping web pages (used in multiple platforms)
beautifulsoup4==4.11.1

# LXML dependency for BeautifulSoup
lxml==4.9.3

# Python-dotenv to load environment variables from .env file
python-dotenv==1.0.0

# aiohttp for asynchronous HTTP requests (if needed)
aiohttp==3.8.3

# FFmpeg Python bindings (for media processing)
ffmpeg-python==0.2.0

# Gaana library for downloading music from Gaana
gaana==0.0.3

# Optional: If you want to handle custom media files (audio, video)
pydub==0.25.1

# Additional dependencies for handling OAuth and logins
oauthlib==3.2.2
requests-oauthlib==1.3.1
