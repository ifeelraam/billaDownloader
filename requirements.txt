python-telegram-bot==20.0  # The version of python-telegram-bot that supports the bot functions
requests==2.28.2           # For making HTTP requests (used in media downloads)
yt-dlp                     # For downloading videos from YouTube and other platforms
pytube==12.1.0             # For downloading YouTube videos (if needed)
pyjiosaavn==1.0.4          # For downloading music from JioSaavn
spotipy==2.18.0            # For interfacing with the Spotify API
instaloader==4.8.9         # For downloading Instagram content (Reels, Stories, etc.)
fb-scraper==0.6.1          # For Facebook video scraping (including private media)
beautifulsoup4==4.11.1     # For parsing HTML (used in scraping)
lxml==4.9.3                # For fast XML and HTML parsing
python-dotenv==1.0.0       # For loading environment variables from .env file
aiohttp==3.8.3             # For asynchronous HTTP requests (for non-blocking media downloads)
ffmpeg-python==0.2.0       # For handling media processing tasks
gaana==0.0.7              # For downloading songs from Gaana
